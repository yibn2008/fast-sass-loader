{
  "name": "fast-sass-loader",
  "description": "fast sass loader for webpack",
  "version": "1.1.0",
  "main": "lib/index.js",
  "scripts": {
    "lint": "standard --fix && npm-ensure -t deps",
    "test": "npm run lint && npm run test-local",
    "test-local": "mocha test/**/*.test.js",
    "test-cov": "istanbul cover _mocha test/**/*.test.js",
    "ci": "npm run lint && npm run test-cov && alicov",
    "release": "npm test && npm-ensure -t changelog && git release $npm_package_version && tnpm publish",
    "precommit": "standard && npm-ensure -t deps"
  },
  "ensure": {
    "deps": {
      "checkDirs": [
        "lib/**/*"
      ]
    }
  },
  "standard": {
    "global": [
      "describe",
      "it",
      "beforeEach",
      "afterEach"
    ],
    "ignore": [
      "/test"
    ]
  },
  "dependencies": {
    "async": "^2.0.1",
    "cli-source-preview": "^1.0.0",
    "co": "^4.6.0",
    "fs-promise": "^0.5.0",
    "loader-utils": "^0.2.15"
  },
  "peerDependencies": {
    "node-sass": "^4.0.0",
    "webpack": "1.x || 2.x"
  },
  "devDependencies": {
    "css-loader": "^0.23.1",
    "extract-text-webpack-plugin": "^2.0.0",
    "file-loader": "^0.9.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "node-sass": "^4.0.0",
    "npm-ensure": "^1.1.0",
    "sass-loader": "^6.0.3",
    "standard": "^9.0.2",
    "webpack": "1.x || 2.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yibn2008/fast-sass-loader.git"
  },
  "keywords": [
    "sass-loader",
    "fast"
  ],
  "author": "yibn2008",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/yibn2008/fast-sass-loader/issues"
  },
  "homepage": "https://github.com/yibn2008/fast-sass-loader#readme"
}
